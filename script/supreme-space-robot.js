square = document.querySelector('.square')

size = 50
direction = "bigger"

int = setInterval(Size, 30)

function Size () {
    if(size = 50) {
        direction = "bigger"
    }
    if(size > 50) {
        direction = "smaller"
    }
    if(direction == bigger) {
        size = size + 1
    }
    if(direction == smaller)
}
